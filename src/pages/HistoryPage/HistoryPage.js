// use the ls for beginning 

function HistoryPage() {
    return (
        // here use the "page" component
        <div>
            <h1>History</h1>
        </div>
    );
};

export default HistoryPage;